# diabetes-dp-fairness
Differentially Private and Fair Diabetes Risk Prediction

If PyTorch installation is too large or causes CUDA-related issues, install the CPU-only PyTorch build from the official PyTorch instructions, then rerun:

pip install -r requirements.txt